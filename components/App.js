/** @jsx React.DOM */

var React = require('react/addons');

var App = React.createClass({
  render: function () {
    return (
      <div>
        React component
      </div>
    )
  }
});

module.exports = App;
